# Python
__pycache__/
*.py[cod]
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
.venv/
env/

# OS / editor
.DS_Store
.vscode/

# Generated artifacts and derived outputs (never committed)
NFTI/artifacts/
NFTI/results/
NFTI/Manuscript/

# Patient-derived data: NEVER commit on any branch. Even de-identified TQIP
# values originate from real patients. Only schema/config under data/schemas/
# is tracked; everything else under data/ (raw cohorts, samples) is ignored.
NFTI/data/*
!NFTI/data/schemas/
